<h1>
  <%= @project.name %>  
</h1>
<h2>Tasks</h2>

<table>
  <tr>
    <th>Title</th>
    <th>Status</th>
    <th>Priority</th>
    <th>Percentage completed</th>
    <th>Due date</th>
    <th>Estimated date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project.tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.status %></td>
    <td><%= task.priority %></td>
    <td><%= task.percentage_completed %></td>
    <td><%= task.due_date %></td>
    <td><%= task.estimated_date %></td>
    <td><%= link_to 'Show', [@project,task] %></td>
    <td><%= link_to 'Edit', edit_project_task_path(@project,task) %></td>
    <td><%= link_to 'Destroy', [@project,task] , method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to_unless !@is_admin,  'New Task', new_project_task_path(@project) %>
<h2>Bugs</h2>

<table>
  <tr>
    <th>Title</th>
    <th>Status</th>
    <th>Priority</th>
    <th>Percentage completed</th>
    <th>Due date</th>
    <th>Estimated date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project.bugs.each do |bug| %>
  <tr>
    <td><%= bug.title %></td>
    <td><%= bug.status %></td>
    <td><%= bug.priority %></td>
    <td><%= bug.percentage_completed %></td>
    <td><%= bug.due_date %></td>
    <td><%= bug.estimated_date %></td>
    <td><%= link_to 'Show', [@project,bug] %></td>
    <td><%= link_to 'Edit', edit_project_bug_path(@project,bug) %></td>
    <td><%= link_to 'Destroy', [@project,bug], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
<br/>
<%= link_to_unless !@is_admin,  'New Bug', new_project_bug_path(@project) %>
<h2>Resources</h2>
<table>
  <tr>
    <th>Title</th>
    <th>Content</th>
    <th>Permissions</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% @project.resources.each do |resource| %>
  <tr>
    <td><%= resource.title %></td>
    <td><%= resource.content %></td>
    <td><%= resource.permissions %></td>
    <td><%= link_to 'Show', [@project,resource] %></td>
    <td><%= link_to 'Edit',  edit_project_resource_path(@project,resource) %></td>
    <td><%= link_to 'Destroy', [@project,resource], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
<br/>

<%= link_to 'New Resource', new_project_resource_path(@project) %>

<h2>Users</h2>

<table>
  <tr>
    <th>User</th>
    <th>Role</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project.user_project_rels.each do |user_project_rel| %>
  <tr>
    <td><%= user_project_rel.username %></td>
    <td><%= user_project_rel.role %></td>
    <td><%= link_to 'Edit', edit_user_project_rel_path(user_project_rel) %></td>
    <td><%= link_to 'Destroy', user_project_rel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add Users', new_project_user_path(@project) %>

 
<%= link_to 'Back', projects_path %>
